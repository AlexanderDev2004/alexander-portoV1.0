---
import Layout from '../layouts/Layout.astro';
import BookmarksSection from '../components/Bookmarks.svelte';
import { bookmarks } from '../data/bookmarks';
---

<Layout>
  <div class="max-w-6xl mx-auto px-4 sm:px-5 md:px-6 py-8 md:py-12">
    <!-- Header -->
    <div class="mb-8 md:mb-12">
      <div class="flex items-start justify-between gap-4 mb-6">
        <div>
          <h1 class="text-3xl sm:text-4xl md:text-5xl font-bold text-gray-900 dark:text-white mb-2">
            Bookmarks
          </h1>
          <p class="text-base sm:text-lg text-gray-600 dark:text-gray-400">
            Today I Learned
          </p>
        </div>
        <a
          href="/"
          class="flex items-center gap-2 px-3 py-2 text-sm font-medium text-gray-600 dark:text-gray-400 hover:text-gray-900 dark:hover:text-white bg-gray-100 dark:bg-gray-800 hover:bg-gray-200 dark:hover:bg-gray-700 rounded-lg transition-colors whitespace-nowrap"
        >
          <iconify-icon icon="mdi:home" width="18" height="18"></iconify-icon>
          <span class="hidden sm:inline">Home</span>
        </a>
      </div>

      <!-- Description -->
      <div class="prose dark:prose-invert max-w-none text-gray-700 dark:text-gray-300">
        <p>
          Well, seems like you've found this not-so-secret page :)
        </p>
        <p>
          This is where I keep my bookmarks or things that I learned. It's meant to be a public archive of things that I find interesting and useful whether it be an article, random thoughts, videos, literally anything. I hope you find something useful here too! These are all coming from my personal collections and research.
        </p>
      </div>
    </div>

    <!-- Bookmarks Content -->
    <BookmarksSection client:load categories={bookmarks} />
  </div>

  <style>
    :global(.prose) {
      font-size: 0.95rem;
      line-height: 1.6;
      margin: 1rem 0;
    }

    :global(.prose p) {
      margin: 0.5rem 0;
    }

    :global(.prose a) {
      color: #3b82f6;
      text-decoration: underline;
    }

    :global(.dark .prose a) {
      color: #60a5fa;
    }

    :global(.prose a:hover) {
      color: #2563eb;
    }

    :global(.dark .prose a:hover) {
      color: #93c5fd;
    }
  </style>
</Layout>
